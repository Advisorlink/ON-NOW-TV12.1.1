<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="32dp"
    android:height="32dp"
    android:viewportWidth="32"
    android:viewportHeight="32">
    <!-- Simple play triangle — matches the design's clean line aesthetic.
         Filled solid so it reads at small sizes. -->
    <path
        android:fillColor="@android:color/white"
        android:pathData="M 9,6 L 9,26 L 25,16 Z" />
</vector>
