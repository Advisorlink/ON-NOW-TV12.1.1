<?xml version="1.0" encoding="utf-8"?>
<!--
    Hero billboard — top row in the shelves list.
    Backdrop fills the full width; gradient layers + a left-anchored
    text column with synopsis, year/genre meta, and a Play pill mirror
    the Vesper React HeroBillboard component.
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="380dp"
    android:layout_marginBottom="8dp">

    <ImageView
        android:id="@+id/hero_backdrop"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:contentDescription="@null" />

    <!-- Bottom-to-top gradient so the title is always readable. -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/hero_gradient" />

    <!-- Right-side fade so left-anchored copy reads clearly. -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#990A0E1A" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="start|bottom"
        android:layout_marginStart="48dp"
        android:layout_marginBottom="42dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/hero_eyebrow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:textColor="@color/vesper_blue"
            android:textSize="12sp"
            android:textStyle="bold"
            android:letterSpacing="0.32"
            android:text="FEATURED" />

        <TextView
            android:id="@+id/hero_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="@color/vesper_text"
            android:textSize="44sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-black"
            android:letterSpacing="-0.03"
            android:maxLines="2"
            android:ellipsize="end"
            android:text="" />

        <TextView
            android:id="@+id/hero_meta"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:textColor="@color/vesper_text_2"
            android:textSize="14sp"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="0.06"
            android:text="" />

        <TextView
            android:id="@+id/hero_synopsis"
            android:layout_width="640dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="14dp"
            android:textColor="@color/vesper_text_2"
            android:textSize="14sp"
            android:lineSpacingExtra="4dp"
            android:maxLines="3"
            android:ellipsize="end"
            android:text="" />

        <LinearLayout
            android:id="@+id/hero_play"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="22dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:background="@drawable/play_button_bg"
            android:paddingStart="22dp"
            android:paddingEnd="26dp"
            android:paddingTop="10dp"
            android:paddingBottom="10dp"
            android:focusable="true"
            android:clickable="true">

            <ImageView
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:src="@drawable/ic_play"
                android:tint="@color/vesper_bg_0"
                android:contentDescription="@null" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:textColor="@color/vesper_bg_0"
                android:textSize="14sp"
                android:textStyle="bold"
                android:letterSpacing="0.10"
                android:text="PLAY" />
        </LinearLayout>
    </LinearLayout>
</FrameLayout>
