<?xml version="1.0" encoding="utf-8"?>
<!--
    Poster tile — 156 × 234 dp (2:3 ratio, standard movie poster).
    Title overlay at the bottom, focus ring on D-pad highlight.
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="156dp"
    android:layout_height="234dp"
    android:layout_marginEnd="14dp"
    android:layout_marginTop="6dp"
    android:layout_marginBottom="6dp"
    android:background="@drawable/poster_tile_bg"
    android:focusable="true"
    android:clickable="true">

    <ImageView
        android:id="@+id/poster_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:contentDescription="@null" />

    <!-- Title fallback when poster is missing. -->
    <TextView
        android:id="@+id/poster_title_fallback"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:padding="14dp"
        android:textColor="@color/vesper_text_2"
        android:textSize="13sp"
        android:textStyle="bold"
        android:maxLines="3"
        android:ellipsize="end"
        android:visibility="gone"
        android:text="" />
</FrameLayout>
