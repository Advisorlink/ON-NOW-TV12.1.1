<?xml version="1.0" encoding="utf-8"?>
<!-- v2.10.7 — Close-app text colour.
     Default: white on translucent.  Focused: still white but on the
     red danger fill so it pops.  Identical states for visual
     simplicity — only the background colour changes. -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:color="#FFFFFF" />
</selector>
