<?xml version="1.0" encoding="utf-8"?>
<!--
  v0.6 — Focused-tile halo.

  Three concentric strokes around the tile rectangle that together
  build a neon-blue glow that LEANS OUTSIDE the tile (negative
  insets) instead of cutting into the image.

  IMPORTANT — for the outer strokes to be visible they need the
  parent RecyclerView and its parent ConstraintLayout to disable
  child clipping:

      android:clipChildren="false"
      android:clipToPadding="false"
      android:padding(Vertical)="20dp"

  Without those, Android crops the halo at the parent bounds and
  the glow re-appears clipped at the top/bottom of the dock.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Outermost soft halo (extends 8 dp outside the tile). -->
    <item
        android:left="-8dp" android:top="-8dp"
        android:right="-8dp" android:bottom="-8dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="24dp" />
            <stroke android:width="6dp" android:color="#1A2BB6FF" />
        </shape>
    </item>

    <!-- Middle halo (extends 4 dp outside the tile). -->
    <item
        android:left="-4dp" android:top="-4dp"
        android:right="-4dp" android:bottom="-4dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="20dp" />
            <stroke android:width="4dp" android:color="#4D2BB6FF" />
        </shape>
    </item>

    <!-- Inner crisp neon ring on the tile edge. -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="16dp" />
            <stroke android:width="2dp" android:color="@color/accent" />
        </shape>
    </item>

</layer-list>
