<?xml version="1.0" encoding="utf-8"?>
<!-- v2.10.57 — One horizontal shelf in the Highfly Sports Guide.
     Title above, horizontal RecyclerView of 16:9 cards below. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingBottom="36dp">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="48dp"
        android:paddingBottom="14dp">

        <View
            android:id="@+id/shelf_live_dot"
            android:layout_width="9dp"
            android:layout_height="9dp"
            android:background="@drawable/highfly_live_dot"
            android:layout_marginEnd="11dp"
            android:visibility="gone" />

        <TextView
            android:id="@+id/shelf_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#F5F8FF"
            android:textSize="22sp"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="-0.01" />

        <TextView
            android:id="@+id/shelf_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#7A8AB2"
            android:textSize="13sp"
            android:fontFamily="monospace"
            android:letterSpacing="0.1"
            android:layout_marginStart="12dp" />
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/shelf_cards"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="48dp"
        android:paddingVertical="12dp"
        android:clipToPadding="false"
        android:scrollbars="none" />

</LinearLayout>
