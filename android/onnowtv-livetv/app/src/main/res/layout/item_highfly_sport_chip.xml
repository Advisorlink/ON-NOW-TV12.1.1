<?xml version="1.0" encoding="utf-8"?>
<!-- v2.10.62 — Cinema-Reel sport-filter chip: horizontal pill
     [icon] [label].  Selected = white fill + black label/icon.
     Default = dark glass + bright label.  Focus = cyan rim. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingHorizontal="14dp"
    android:paddingVertical="9dp"
    android:layout_marginEnd="10dp"
    android:focusable="true"
    android:clickable="true"
    android:background="@drawable/highfly_sport_chip_bg">

    <ImageView
        android:id="@+id/sport_chip_icon"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_marginEnd="8dp"
        android:contentDescription="@null"
        app:tint="#F5F8FF" />

    <TextView
        android:id="@+id/sport_chip_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#F5F8FF"
        android:textSize="12sp"
        android:fontFamily="sans-serif-medium"
        android:singleLine="true" />
</LinearLayout>
