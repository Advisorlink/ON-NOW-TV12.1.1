<?xml version="1.0" encoding="utf-8"?>
<!--
    Channel pill card (middle column).  Logo + channel number, name,
    NOW programme title, cyan progress bar.  Focused state shows the
    cyan border + brighter bg.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="86dp"
    android:layout_marginBottom="8dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/channel_pill_bg"
    android:focusable="true"
    android:clickable="true"
    android:padding="12dp">

    <FrameLayout
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:background="@drawable/channel_logo_bg">

        <ImageView
            android:id="@+id/ch_logo"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:layout_gravity="center"
            android:scaleType="fitCenter"
            android:contentDescription="@null" />
    </FrameLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="14dp"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/ch_num"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:fontFamily="monospace"
                android:textColor="@color/livetv_accent"
                android:textSize="11sp"
                android:textStyle="bold"
                android:letterSpacing="0.22"
                android:text="" />

            <TextView
                android:id="@+id/ch_name"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="@color/livetv_fg"
                android:textSize="14sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-medium"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="" />

            <ImageView
                android:id="@+id/ch_fav_marker"
                android:layout_width="14dp"
                android:layout_height="14dp"
                android:layout_marginStart="6dp"
                android:visibility="gone"
                android:contentDescription="@null" />
        </LinearLayout>

        <TextView
            android:id="@+id/ch_now_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:textColor="@color/livetv_fg_dim"
            android:textSize="12sp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="" />

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:layout_marginTop="8dp"
            android:background="#1AFFFFFF">

            <View
                android:id="@+id/ch_progress"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/livetv_accent" />
        </FrameLayout>
    </LinearLayout>
</LinearLayout>
