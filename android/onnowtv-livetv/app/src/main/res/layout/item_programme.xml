<?xml version="1.0" encoding="utf-8"?>
<!--
    Programme cell.  Width is set programmatically (12 px / minute
    times the programme duration).  Focused state owns the red
    border via `programme_bg.xml`.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:layout_marginEnd="3dp"
    android:layout_marginTop="5dp"
    android:layout_marginBottom="5dp"
    android:orientation="vertical"
    android:background="@drawable/programme_bg"
    android:focusable="true"
    android:clickable="true"
    android:paddingStart="8dp"
    android:paddingEnd="8dp"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

    <TextView
        android:id="@+id/programme_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/livetv_fg"
        android:textSize="13sp"
        android:textStyle="bold"
        android:fontFamily="sans-serif-medium"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="" />

    <TextView
        android:id="@+id/programme_time"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:textColor="@color/livetv_fg_dim"
        android:textSize="10sp"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="" />
</LinearLayout>
