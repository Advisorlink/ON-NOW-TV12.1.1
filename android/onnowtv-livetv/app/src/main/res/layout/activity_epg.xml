<?xml version="1.0" encoding="utf-8"?>
<!--
    EPG screen layout — mirrors the FTA design.

      • Header row at top: "Today" + time slots (12 px / minute).
      • Body below: a vertical RecyclerView of channel rows.  Each
        row has the channel logo / LCN on the LEFT, then a horizontal
        RecyclerView of programme cells on the RIGHT.
      • Sidebar on the LEFT (independent): live preview area + info
        card showing the focused programme.
      • Vertical red NOW line drawn over the programme grid via the
        custom NowLineOverlay view.
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/epg_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/livetv_bg"
    android:focusable="false">

    <!-- Top-right clock strip -->
    <LinearLayout
        android:id="@+id/clock_strip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="22dp"
        android:layout_marginEnd="32dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/category_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="18dp"
            android:textColor="#8FB0B8C8"
            android:textSize="12sp"
            android:textStyle="bold"
            android:letterSpacing="0.20"
            android:fontFamily="sans-serif-medium"
            android:focusable="true"
            android:clickable="true"
            android:padding="6dp"
            android:background="@drawable/category_bg"
            android:text="ALL" />

        <TextView
            android:id="@+id/clock"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/livetv_fg"
            android:textSize="22sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif"
            tools:ignore="HardcodedText"
            xmlns:tools="http://schemas.android.com/tools"
            android:text="" />
    </LinearLayout>

    <!-- Body: sidebar on left, EPG grid on right. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:paddingTop="64dp">

        <!-- Sidebar (preview + info card) -->
        <LinearLayout
            android:id="@+id/sidebar"
            android:layout_width="420dp"
            android:layout_height="match_parent"
            android:paddingStart="28dp"
            android:paddingEnd="20dp"
            android:paddingTop="8dp"
            android:paddingBottom="24dp"
            android:orientation="vertical">

            <!-- Preview pane (16:9) -->
            <FrameLayout
                android:id="@+id/preview_frame"
                android:layout_width="match_parent"
                android:layout_height="218dp"
                android:background="@color/livetv_bg_card"
                android:focusable="true"
                android:focusableInTouchMode="false"
                android:clickable="true"
                android:nextFocusRight="@+id/programme_rows">

                <ImageView
                    android:id="@+id/preview_art"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    android:contentDescription="@null" />

                <TextView
                    android:id="@+id/live_pill"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="12dp"
                    android:layout_marginTop="12dp"
                    android:background="@drawable/live_pill"
                    android:paddingStart="10dp"
                    android:paddingEnd="10dp"
                    android:paddingTop="4dp"
                    android:paddingBottom="4dp"
                    android:text="@string/live_pill"
                    android:textColor="@color/livetv_fg"
                    android:textSize="11sp"
                    android:textStyle="bold"
                    android:letterSpacing="0.18" />

                <TextView
                    android:id="@+id/preview_hint"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="top|end"
                    android:layout_marginEnd="12dp"
                    android:layout_marginTop="12dp"
                    android:textColor="@color/livetv_fg"
                    android:textSize="11sp"
                    android:letterSpacing="0.14"
                    android:fontFamily="sans-serif-medium"
                    android:text="PRESS OK TO PLAY" />
            </FrameLayout>

            <!-- Channel logo + LCN + name -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <ImageView
                    android:id="@+id/info_logo"
                    android:layout_width="56dp"
                    android:layout_height="56dp"
                    android:contentDescription="@null"
                    android:scaleType="fitCenter" />

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="14dp"
                    android:layout_weight="1"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/info_lcn"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textColor="@color/livetv_fg"
                        android:textSize="22sp"
                        android:textStyle="bold"
                        android:fontFamily="sans-serif-medium"
                        android:text="" />

                    <TextView
                        android:id="@+id/info_name"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textColor="@color/livetv_fg_dim"
                        android:textSize="14sp"
                        android:text="" />
                </LinearLayout>
            </LinearLayout>

            <TextView
                android:id="@+id/info_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="14dp"
                android:textColor="@color/livetv_fg"
                android:textSize="18sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-medium"
                android:text="" />

            <TextView
                android:id="@+id/info_time"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:textColor="@color/livetv_fg_dim"
                android:textSize="13sp"
                android:text="" />

            <TextView
                android:id="@+id/info_synopsis"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:textColor="@color/livetv_fg_dim"
                android:textSize="13sp"
                android:lineSpacingExtra="3dp"
                android:maxLines="5"
                android:ellipsize="end"
                android:text="" />
        </LinearLayout>

        <!-- Right pane: time strip + programme rows -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- Time header strip -->
            <FrameLayout
                android:id="@+id/time_strip_frame"
                android:layout_width="match_parent"
                android:layout_height="42dp"
                android:paddingStart="104dp"
                android:clipChildren="false"
                android:clipToPadding="false">

                <HorizontalScrollView
                    android:id="@+id/time_strip_scroll"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scrollbars="none"
                    android:fillViewport="false"
                    android:focusable="false"
                    android:descendantFocusability="blocksDescendants"
                    android:overScrollMode="never">

                    <LinearLayout
                        android:id="@+id/time_strip"
                        android:layout_width="wrap_content"
                        android:layout_height="match_parent"
                        android:orientation="horizontal" />
                </HorizontalScrollView>
            </FrameLayout>

            <!-- Vertical RecyclerView of channel rows -->
            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:clipChildren="false"
                android:clipToPadding="false">

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/programme_rows"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:clipChildren="false"
                    android:clipToPadding="false"
                    android:descendantFocusability="afterDescendants"
                    android:focusable="true" />

                <!-- Custom view that draws the red NOW line on top
                     of the rows.  Doesn't intercept any input. -->
                <tv.onnowtv.livetv.ui.NowLineOverlay
                    android:id="@+id/now_line"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:focusable="false"
                    android:clickable="false" />
            </FrameLayout>
        </LinearLayout>
    </LinearLayout>

    <!--
        Categories drawer overlay.  Slides in from the left and
        occupies the full height.  Opens with the MENU remote key
        or BACK from the EPG; closes when an item is picked or
        BACK is pressed again.
    -->
    <FrameLayout
        android:id="@+id/categories_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#CC06080F"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:clickable="true"
        android:visibility="gone">

        <LinearLayout
            android:layout_width="380dp"
            android:layout_height="match_parent"
            android:background="@color/livetv_bg_card"
            android:orientation="vertical"
            android:elevation="12dp">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingStart="20dp"
                android:paddingEnd="20dp"
                android:paddingTop="22dp"
                android:paddingBottom="14dp"
                android:textColor="@color/livetv_accent"
                android:textSize="11sp"
                android:textStyle="bold"
                android:letterSpacing="0.22"
                android:fontFamily="sans-serif-medium"
                android:text="CATEGORIES" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/categories_list"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:descendantFocusability="afterDescendants"
                android:focusable="true" />
        </LinearLayout>
    </FrameLayout>
</FrameLayout>
