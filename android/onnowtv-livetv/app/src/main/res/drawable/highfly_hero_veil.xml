<?xml version="1.0" encoding="utf-8"?>
<!-- v2.10.62 — Cinema-Reel hero veil: top translucent (so the
     poster reads), centre & bottom heavy so the title is legible
     and we transition into the dark bottom section. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:startColor="#66000000"
        android:centerColor="#7A000000"
        android:endColor="#04060B"
        android:angle="270"
        android:type="linear" />
</shape>
