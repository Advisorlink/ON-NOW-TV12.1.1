<?xml version="1.0" encoding="utf-8"?>
<!-- Tall radial overlay that sits on top of the AI cover art so the
     bottom name+count strip remains legible regardless of which
     specific artwork GPT-Image-1 returned. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:angle="90"
        android:startColor="#00000000"
        android:centerColor="#33000000"
        android:endColor="#CC000000" />
</shape>
